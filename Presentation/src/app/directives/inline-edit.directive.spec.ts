import { InlineEditDirective } from "./inline-edit.directive";

describe("EditDirective", () => {
  it("should create an instance", () => {
    const directive = new InlineEditDirective();
    expect(directive).toBeTruthy();
  });
});
